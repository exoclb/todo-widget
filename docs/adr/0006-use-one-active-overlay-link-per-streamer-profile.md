# Use one active Overlay Link per Streamer Profile

For the hosted platform MVP, one active Overlay Link maps to one Streamer Profile and renders that profile's complete saved Overlay State through a read-only Hosted Overlay route. Inactive Overlay Links render an empty or disabled overlay shell without exposing Overlay State or streamer-specific data, while Regenerated Overlay Link lifecycle behavior remains future work.
